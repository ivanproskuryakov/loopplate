'use strict';

define(['app'], function (app) {
  app.controller(
    'StreamCtrl', [
      '$location',
      '$scope',
      '$rootScope',
      function ($location,
                $scope,
                $rootScope) {
      }
    ]);
});
